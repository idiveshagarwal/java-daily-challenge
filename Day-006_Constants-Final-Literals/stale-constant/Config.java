/** Change VERSION to 2, recompile ONLY this file, then re-run App. */
public class Config {

    /** Compile-time constant — javac inlines this into every reader. */
    public static final int VERSION = 1;
}
