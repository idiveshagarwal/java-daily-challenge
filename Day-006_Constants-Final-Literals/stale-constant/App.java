/** Reads Config.VERSION. The value is baked into App.class at compile time. */
public class App {

    public static void main(String[] args) {
        System.out.println("Config.VERSION = " + Config.VERSION);
    }
}
